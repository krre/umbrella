pub const Pos2D = struct {
    x: i32,
    y: i32,
};

pub const Size2D = struct {
    width: u32,
    height: u32,
};

pub const Pos3D = struct {
    x: f32,
    y: f32,
    z: f32,
};

pub const Size3D = struct {
    width: f32,
    height: f32,
    depth: f32,
};
